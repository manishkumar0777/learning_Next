
export const metadata = {
    title : "Counter",
}

import { Counter } from "./Counter"

export default function CounterPage(){
    return (
        <div>
            <Counter />
        </div>
    )
}